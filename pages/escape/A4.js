import EscapeForm from "../../components/form";

export default function A4() {

    return (
        <EscapeForm
            title="A4"
            instruction="Submit your answer from A4."
            answer={"game"}
            next="You are done with Part A! The first number of the combination lock is A=12."
        />
    )
}